server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Client-side routing (react-router-dom): unknown paths fall back to
    # index.html instead of a raw nginx 404, so a hard refresh on
    # /students or /fees still loads the app.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
